const allhelp = {
    "title":"This program displays a greeting. "+
            "The function <span class='cx-fun'>hello()</span> is defined, which takes one parameter (<span class='cx-var'>nameParameter</span>). "+
            "This example shows how to define and call a function.",
     "4":"The <span class='cx-fun'>hello()</span> function's signature defines the name of the function, "+
         "as well as defining that the function has one input parameter named <span class='cx-var'>nameParameter</span>.",
     "5":"A greeting is display to the screen, using input parameter <span class='cx-var'>nameParameter</span>.",

     "7": "Get input for the user's name, "+
         "and assign to variable <span class='cx-var'>name</span>. ",
     "8":"Call the <span class='cx-fun'>hello()</span> function, providing it an argument (the <span class='cx-var'>name</span> variable). ",

     "hello.nameParameter": "The parameter <span class='cx-var'>nameParameter</span> is the input to the <span class='cx-fun'>hello()</span> function. "+
                              "Note this variable is known only within the <span class='cx-fun'>hello()</span> function, "+
                              "but not known elsewhere in the program.",
     "name":"This variable stores the user's name, as entered by the user.",

     "display": "-",
     "keyboard": "-" 
};
  