// p4da-2-6-help.js
const allhelp = {
    "title":"This program calculates body mass index (BMI) based on <span class='cx-var'>height</span> and <span class='cx-var'>weight</span>. "+
            "Height and weight are prompted for and entered by the user, "+
            "then bmi is calculated and displayed. " +
            "The program also shows how to convert user input to a numeric value "+
            "using a combination of the <span class='cx-fun'>input()</span> and <span class='cx-fun'>float()</span> functions. " +
            "Another function shown is <span class='cx-fun'>print()</span>, for displaying output to the screen.",
    "4": "Get input for height, convert to float, "+
         "and assign to variable <span class='cx-var'>height</span>. "+ 
         "The <span class='cx-fun'>input()</span> function returns a string, which is converted to a number "+
         "by the <span class='cx-fun'>float()</span> function, before being assigned to the <span class='cx-var'>height</span> variable.",
    "5": "Get input for weight, convert to float, "+
         "and assign to variable <span class='cx-var'>weight</span>. "+
         "The <span class='cx-fun'>input()</span> function returns a string, which is converted to a number " +
         "by the <span class='cx-fun'>float()</span> function, before being assigned to the <span class='cx-var'>weight</span> variable.",
    "8": "Calculate <span class='cx-var'>bmi</span> based on "+
         "<span class='cx-var'>weight</span> and "+
         "<span class='cx-var'>height</span>.",
    "11": "Display the calculated BMI.  The <span class='cx-fun'>print()</span> function here displays "+
          "both a string, as well as the value stored in the variable "+
          "<span class='cx-var'>bmi</span>.",
    "height": "This variable stores height (in inches). "+
              "It is entered by the user and used subsequently in the calculation of BMI.",
    "weight": "This variable stores weight (in pounds). "+
              "It is entered by the user and used subsequently in the calculation of BMI.",
    "bmi": "This variable stores bmi. "+
           "It is calculated based on <span class='cx-var'>height</span> and <span class='cx-var'>weight</span>, and subsequently displayed at the end of the program."
  };
  