// p4da-2-5-help.js
const allhelp = {
    "title":"This program calculates the total of two numbers. "+
            "value1 and value2 are prompted for and entered by the user, "+
            "then the total is calculated and displayed. " +
            "The program also shows how to convert user input to a numeric value "+
            "using a combination of the input() and float() functions. " +
            "Another function shown is print(), for displaying output to the screen.",
    "4": "Prompt and get input for a value, convert to float, "+
         "and assign to variable <span class='cx-var'>value1</span>. "+
         "The steps in this statement are:<ul>"+ 
         "<li>The <span class='cx-fun'>input()</span> function returns a string (that was entered by the user),</li>"+
         "<li>which is converted to a number</li> "+
         "by the <span class='cx-fun'>float()</span> function,"+
         "<li>which is assigned to the <span class='cx-var'>value1</span> variable.</li></ul>",
    "5": "Prompt and get input for a value, convert to float, "+
         "and assign to variable <span class='cx-var'>value2</span>. "+
         "The <span class='cx-fun'>input()</span> function returns a string, which is converted to a number " +
         "by the <span class='cx-fun'>float()</span> function, before being assigned to the <span class='cx-var'>value2</span> variable.",
    "8": "Calculate <span class='cx-var'>total</span> based on "+
         "<span class='cx-var'>value1</span> and "+
         "<span class='cx-var'>value2</span>.",
    "11": "Display the calculated total.  The <span class='cx-fun'>print()</span> function here displays "+
          "the value stored in the variable "+
          "<span class='cx-var'>total</span>.",
    "value1": "This variable stores the first value entered by the user. "+
              "It is used subsequently in the calculation of <span class='cx-var'>total</span>.",
    "value2": "This variable stores the second value entered by the user. "+
              "It is used subsequently in the calculation of <span class='cx-var'>total</span>.",
    "total": "This variable stores the total. "+
           "It is calculated based on <span class='cx-var'>value1</span> and <span class='cx-var'>value2</span>, and subsequently displayed at the end of the program.",

     "display:": "-",
     "keyboard": "-"      
  };
  