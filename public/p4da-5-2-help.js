// p4da-5-2-help.js
const allhelp = { 
    "title":"This program calculates total order cost based on a number of books. "+
            "The function calcCost() is defined, which takes one parameter (numberItems), "+
            "and returns the total cost.  This example shows how to define and call a function.",
     "4":"The calcTotal function's signature defines the name of the function, "+
         "as well as defining that the function has one input parameter named numberItems.",
     "5":"The variable totalCost is calculated based on the input parameter numberItems.",
     "6":"The result value of the function, totalCost, is returned to the calling statement.",
     "9": "Get input for count, convert to an integer value, "+
         "and assign to variable <span class='cx-var'>count</span>. "+ 
         "The input() function returns a string, which is converted to a number "+
         "by the int() function, before being assigned to the count variable.",
     "12":"Call the calcTotal() function, providing it an argument (the count variable). "+
          "Then, receive the result from calcTotal() and assign it to the total variable.",
     "15": "Display the calculated total cost.  The print() function here displays "+
     "both a string, as well as the value stored in the variable "+
     "<span class='cx-var'>total</span>.",

     "calcTotal.numberItems": "The parameter numberItems is the input to the calcTotal() function. "+
                              "Note this variable is known only within the calcTotal() function, "+
                              "but not known elsewhere in the program.",
     "calcTotal.totalCost": "This variable is the calculated cost based on the passed-in parameter numberItems and "+
                            "the price per book ($17.50).  "+
                            "Note this variable is known only within the calcTotal() function, "+
                            "but not known elsewhere in the program.",
     "count":"This variable stores the number of books, as entered by the user.",
     "total":"This variable is the total cost for the purchased books."
};
  