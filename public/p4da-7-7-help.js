const allhelp = {
    "title":"This program writes a line of a text into a file. "+
            "The <span class='cx-fun'>open()</span> function is used to associate the file with a variable, "+
            "and through this variable, the <span class='cx-fun'>print()</span> function is called to write one line to the file.",
    "3": "Prompt the user for their name, and store it as a string in the variable <span class='cx-var'>name</span>.",
    "6": "The <span class='cx-fun'>open()</span> function associates the variable <span class='cx-var'>fout</span> with the file named greeting.txt. "+
         " The variable <span class='cx-var'>fout</span> will be used in a subsequent statement to write the data to the file. "+
         "The second argument to <span class='cx-fun'>open()</span>, <span class='cx-str'>'w'</span>, indicates the file will be written to (as opposed to read from).",
    "9": "Write a line to the file.",
    "12": "After the line has been written to the file, close the file.",

    "f": "This variable is a reference to the data file.",
    "name": "This variable stores the name of the user.",

     "display": "-", 
     "keyboard": "-",     
     "file": "-"      
  };
  